services:
  postgres:
    restart: "no"

  mid:
    restart: "no"
    ports:
      - "${HOST_API_BIND:-127.0.0.1}:${HOST_API_PORT:-8787}:8787"

  caddy:
    restart: "no"
    profiles:
      - public

