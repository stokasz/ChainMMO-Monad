# Ops env (Docker Compose / Coolify)
#
# Copy to a private file on the server (example: /opt/chainmmo/repo/ops/.env)
# Never commit secrets.

# TLS / Domain
TLS_CONTACT_EMAIL=you@example.com

# Database
POSTGRES_PASSWORD=change-me

# Middleware mode
MID_MODE=read-only
LOG_LEVEL=info

# Required unless MID_MODE=read-only (hot wallet signer for action tools)
SIGNER_PRIVATE_KEY=

# Chain
CHAIN_RPC_URL=https://...
# 10143=testnet, 143=mainnet, 31337=devnet
CHAIN_ID=10143
CHAIN_CONFIRMATIONS=2
CHAIN_START_BLOCK=1

# Contracts (fill from deployments/contracts.latest.json)
GAMEWORLD_ADDRESS=0x0000000000000000000000000000000000000000
FEEVAULT_ADDRESS=0x0000000000000000000000000000000000000000
ITEMS_ADDRESS=0x0000000000000000000000000000000000000000
MMO_ADDRESS=0x0000000000000000000000000000000000000000
TRADE_ESCROW_ADDRESS=0x0000000000000000000000000000000000000000
RFQ_MARKET_ADDRESS=0x0000000000000000000000000000000000000000
# Optional legacy field; leave blank in external-token/no-faucet mode.
MMODISTRIBUTOR_ADDRESS=

# Optional API key (gates /metrics only; public reads are open)
API_KEY=

# X OAuth 1.0a (Sign in with X) for verified wallet <-> X linking.
# X_CONSUMER_KEY=
# X_CONSUMER_SECRET=
# X_OAUTH_CALLBACK_URL=https://test.chainmmo.com/auth/x/callback
# X_WEB_ORIGIN=https://test.chainmmo.com

# MCP onboarding gas stipend (server-side)
# Optional dedicated faucet key for MCP onboarding. Leave blank to use SIGNER_PRIVATE_KEY.
MCP_STIPEND_WALLET_PRIVATE_KEY=
MCP_STIPEND_ENABLED=false
MCP_STIPEND_AMOUNT_WEI=100000000000000000
MCP_STIPEND_MIN_BALANCE_WEI=100000000000000000
MCP_STIPEND_WALLET_MIN_BALANCE_WEI=200000000000000000
MCP_STIPEND_ADDRESS_COOLDOWN_SECONDS=900
MCP_STIPEND_PER_ADDRESS_LIMIT_PER_WINDOW=2
MCP_STIPEND_GLOBAL_PER_HOUR_LIMIT=60

# Grok Arena (optional).
# For devnet you can point this at a local OpenClaw gateway on the same machine.
# Mainnet expectation: use a server-hosted gateway reachable from deployed middleware (not localhost).
GROK_ARENA_ENABLED=false
GROK_OPENCLAW_GATEWAY_URL=
GROK_OPENCLAW_GATEWAY_TOKEN=
GROK_OPENCLAW_MAX_TOKENS=1024
GROK_OPENCLAW_MAX_COMPLETION_TOKENS=1024
GROK_OPENCLAW_CLIENT_ID=gateway-client
GROK_OPENCLAW_CLIENT_DISPLAY_NAME=
GROK_OPENCLAW_CLIENT_MODE=backend
GROK_OPENCLAW_CLIENT_PLATFORM=
GROK_OPENCLAW_CLIENT_VERSION=1.0.0
GROK_OPENCLAW_CLIENT_LOCALE=en-US
GROK_OPENCLAW_CLIENT_SCOPES=operator.admin,operator.approvals,operator.pairing,operator.write
GROK_OPENCLAW_REQUEST_TIMEOUT_MS=15000
GROK_GATEWAY_READY_TIMEOUT_MS=5000
GROK_AGENT_ID=chainmmo
GROK_AGENT_ADDRESS=

# Optional (internal/on-demand stacks that bind localhost)
# - on-demand override default is 8787
# - Coolify internal bring-up default is 8788 (see ops/docker-compose.coolify-internal.yml)
HOST_API_BIND=127.0.0.1
HOST_API_PORT=8787
