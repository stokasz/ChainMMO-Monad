services:
  postgres:
    image: postgres:15-alpine
    restart: unless-stopped
    cpus: "0.75"
    mem_limit: "1024m"
    mem_reservation: "256m"
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
    environment:
      POSTGRES_DB: chainmmo
      POSTGRES_USER: chainmmo
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U chainmmo -d chainmmo"]
      interval: 5s
      timeout: 5s
      retries: 20

  mid:
    build:
      context: ..
      dockerfile: mid/Dockerfile
    restart: unless-stopped
    cpus: "1.25"
    mem_limit: "1536m"
    mem_reservation: "384m"
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
    depends_on:
      postgres:
        condition: service_healthy
    # Allows containers to reach host services (Anvil on devnet) via `host.docker.internal`.
    # On Linux this requires Docker's `host-gateway` support (Docker 20.10+). On Docker Desktop it is a no-op.
    extra_hosts:
      - "host.docker.internal:host-gateway"
    environment:
      NODE_ENV: production
      MID_MODE: ${MID_MODE:-read-only}
      LOG_LEVEL: ${LOG_LEVEL:-info}
      SIGNER_PRIVATE_KEY: ${SIGNER_PRIVATE_KEY:-}

      CHAIN_RPC_URL: ${CHAIN_RPC_URL}
      CHAIN_ID: ${CHAIN_ID}
      CHAIN_CONFIRMATIONS: ${CHAIN_CONFIRMATIONS:-2}
      CHAIN_START_BLOCK: ${CHAIN_START_BLOCK:-1}

      GAMEWORLD_ADDRESS: ${GAMEWORLD_ADDRESS}
      FEEVAULT_ADDRESS: ${FEEVAULT_ADDRESS}
      ITEMS_ADDRESS: ${ITEMS_ADDRESS}
      MMO_ADDRESS: ${MMO_ADDRESS}
      TRADE_ESCROW_ADDRESS: ${TRADE_ESCROW_ADDRESS}
      RFQ_MARKET_ADDRESS: ${RFQ_MARKET_ADDRESS}
      MMODISTRIBUTOR_ADDRESS: ${MMODISTRIBUTOR_ADDRESS:-}

      DATABASE_URL: postgres://chainmmo:${POSTGRES_PASSWORD}@postgres:5432/chainmmo

      API_HOST: 0.0.0.0
      API_PORT: 8787
      API_KEY: ${API_KEY:-}
      MCP_STIPEND_WALLET_PRIVATE_KEY: ${MCP_STIPEND_WALLET_PRIVATE_KEY:-}
      MCP_STIPEND_ENABLED: ${MCP_STIPEND_ENABLED:-false}
      MCP_STIPEND_AMOUNT_WEI: ${MCP_STIPEND_AMOUNT_WEI:-100000000000000000}
      MCP_STIPEND_MIN_BALANCE_WEI: ${MCP_STIPEND_MIN_BALANCE_WEI:-100000000000000000}
      MCP_STIPEND_WALLET_MIN_BALANCE_WEI: ${MCP_STIPEND_WALLET_MIN_BALANCE_WEI:-200000000000000000}
      MCP_STIPEND_ADDRESS_COOLDOWN_SECONDS: ${MCP_STIPEND_ADDRESS_COOLDOWN_SECONDS:-900}
      MCP_STIPEND_PER_ADDRESS_LIMIT_PER_WINDOW: ${MCP_STIPEND_PER_ADDRESS_LIMIT_PER_WINDOW:-2}
      MCP_STIPEND_GLOBAL_PER_HOUR_LIMIT: ${MCP_STIPEND_GLOBAL_PER_HOUR_LIMIT:-60}
      CHAIN_PUBLIC_RPC_URLS: ${CHAIN_PUBLIC_RPC_URLS:-}
      CHAIN_PUBLIC_RPC_URL: ${CHAIN_PUBLIC_RPC_URL:-}

      # X OAuth 1.0a (Sign in with X) for verified wallet <-> X linking.
      X_CONSUMER_KEY: ${X_CONSUMER_KEY:-}
      X_CONSUMER_SECRET: ${X_CONSUMER_SECRET:-}
      X_OAUTH_CALLBACK_URL: ${X_OAUTH_CALLBACK_URL:-}
      X_WEB_ORIGIN: ${X_WEB_ORIGIN:-}

      # Grok Arena (optional). For devnet this can point at a local OpenClaw gateway on the same host.
      # Mainnet expectation: use a server-hosted gateway reachable from the deployed middleware (do not point at a laptop/localhost).
      GROK_ARENA_ENABLED: ${GROK_ARENA_ENABLED:-false}
      GROK_OPENCLAW_GATEWAY_URL: ${GROK_OPENCLAW_GATEWAY_URL:-}
      GROK_OPENCLAW_GATEWAY_TOKEN: ${GROK_OPENCLAW_GATEWAY_TOKEN:-}
      GROK_OPENCLAW_REQUEST_TIMEOUT_MS: ${GROK_OPENCLAW_REQUEST_TIMEOUT_MS:-15000}
      GROK_GATEWAY_READY_TIMEOUT_MS: ${GROK_GATEWAY_READY_TIMEOUT_MS:-5000}
      GROK_AGENT_ID: ${GROK_AGENT_ID:-chainmmo}
      GROK_AGENT_ADDRESS: ${GROK_AGENT_ADDRESS:-}
      GROK_PROMPT_MAX_CHARS: ${GROK_PROMPT_MAX_CHARS:-600}
      GROK_HISTORY_LIMIT: ${GROK_HISTORY_LIMIT:-10}
      GROK_RATE_LIMIT_COOLDOWN_SECONDS: ${GROK_RATE_LIMIT_COOLDOWN_SECONDS:-10}
      GROK_RATE_LIMIT_PER_HOUR: ${GROK_RATE_LIMIT_PER_HOUR:-20}
      GROK_IP_HASH_SALT: ${GROK_IP_HASH_SALT:-}
    healthcheck:
      test:
        [
          "CMD",
          "node",
          "-e",
          "fetch('http://127.0.0.1:8787/health').then(r=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))"
        ]
      interval: 10s
      timeout: 5s
      retries: 20
      start_period: 20s

  caddy:
    build:
      context: .
      dockerfile: Caddy.Dockerfile
    restart: unless-stopped
    cpus: "0.25"
    mem_limit: "256m"
    mem_reservation: "64m"
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
    depends_on:
      mid:
        condition: service_healthy
    environment:
      TLS_CONTACT_EMAIL: ${TLS_CONTACT_EMAIL}
    ports:
      - "80:80"
      - "443:443"
    networks:
      - default
      - coolify
    volumes:
      - caddy_data:/data
      - caddy_config:/config
    healthcheck:
      test: ["CMD", "curl", "-fsS", "-o", "/dev/null", "http://127.0.0.1:2019/config/"]
      interval: 10s
      timeout: 5s
      retries: 20
      start_period: 10s

volumes:
  postgres_data:
  caddy_data:
  caddy_config:

networks:
  # Allows Caddy to reverse-proxy Coolify webhook endpoints without exposing Coolify UI ports publicly.
  # The Coolify installer creates this network. If it's missing, create it on the host with:
  #   docker network create coolify
  coolify:
    external: true
    name: coolify
